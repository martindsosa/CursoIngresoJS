function mostrar()
{
	for (var contador=1; contador <= 10;contador ++ ){ //ejercicio con la estructura del for
		console.log(contador);
	}
	
	contador = 0;
	do{// con la estructura del DO WHILE
		contador++;
		console.log(contador);
	}while(contador < 10);
}